#! /bin/sh
set -eu

if test "$#" -gt 0
then
  fixable_files="fixable-branch-files $1"
else
  fixable_files="fixable-recent-files"
fi

$fixable_files |            xargs -n 1 -d '\n' editorconfig-tools fix
$fixable_files | grep php | xargs -n 1 -d '\n' phpcbf --standard=psr2
